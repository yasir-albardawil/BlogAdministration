@model Category

@{
    ViewData["Title"] = "Category Details";
}

<h1 class="mt-4">Category Details</h1>

<div class="card mt-4">
    <div class="card-header">
        Category Information
    </div>
    <div class="card-body">
        <h4 class="card-title">@Model.CategoryName</h4>
        <hr />
        <div class="row mb-3">
            <div class="col-sm-3 fw-bold">
                @Html.DisplayNameFor(model => model.CategoryId)
            </div>
            <div class="col-sm-9">
                @Html.DisplayFor(model => model.CategoryId)
            </div>
        </div>
        <div class="row mb-3">
            <div class="col-sm-3 fw-bold">
                @Html.DisplayNameFor(model => model.CategoryName)
            </div>
            <div class="col-sm-9">
                @Html.DisplayFor(model => model.CategoryName)
            </div>
        </div>
        <div class="row mb-3">
            <div class="col-sm-3 fw-bold">
                @Html.DisplayNameFor(model => model.Description)
            </div>
            <div class="col-sm-9">
                @Html.DisplayFor(model => model.Description)
            </div>
        </div>

 @*        <h5 class="mt-4">Items in this Category</h5>
        @if (Model.Items != null && Model.Items.Any())
        {
            <table class="table table-striped mt-3">
                <thead>
                    <tr>
                        <th>Item Name</th>
                        <th>Price</th>
                        <th>Is Item of the Week</th>
                        <th>In Stock</th>
                        <th>Details</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var pie in Model.Items)
                    {
                        <tr>
                            <td>@pie.Name</td>
                            <td>@pie.Price.ToString("C")</td>
                            <td>@pie.IsPieOfTheWeek ? "Yes" : "No"</td>
                            <td>@pie.InStock ? "Yes" : "No"</td>
                            <td>
                                <a asp-controller="Item" asp-action="Details" asp-route-id="@pie.Id" class="btn btn-primary btn-sm">View</a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        }
        else
        {
            <p>No items found for this category.</p>
        } *@

        <a asp-action="List" class="btn btn-primary mt-4">Back to List</a>
    </div>
</div>
